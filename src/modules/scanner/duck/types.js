/**
 * List of types used in actions and reducers.
 * Every type value should be prefixed by "<module_name>/"
 */

const EXAMPLE_ACTION = 'example/EXAMPLE_ACTION'

export default {
  EXAMPLE_ACTION,
}
